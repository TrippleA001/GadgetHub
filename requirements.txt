streamlit>=1.30.0  # Upgrade Streamlit to a more recent version
                   # 1.30.0+ is generally good for Python 3.12+ compatibility
                   # You can even just put 'streamlit' to get the very latest.
pandas
git+https://github.com/streamlit/gsheets-connection  # Keep this, but its compatibility is the core issue
numpy>=1.26.0      # Ensures a NumPy version compatible with Python 3.12+
pillow>=10.0.0     # Ensures a Pillow version compatible with Python 3.12+
